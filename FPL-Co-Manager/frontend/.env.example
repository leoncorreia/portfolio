# Optional: override API base in production (default: Vite dev server proxies /api to backend)
# VITE_API_URL=http://127.0.0.1:8000

# Dev: where Vite proxies /api and /health (match backend uvicorn port; default 8000)
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8001
